<li id="character-<%= character.id %>" class="character-tag btn btn-warning">
  <div class="character-icon">
    <i class="fa-solid fa-2x fa-person"></i>
  </div>
  <div class="character-name"><%= character.name %></div>
</li>

<style>
.character-tag {
  display: flex;
  font-size: small;
  .character-icon {
  }
  .character-name {
    position: relative;
    left: 5px;
    top: 3px;
  }
}
</style>